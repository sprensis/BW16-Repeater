# Bridge vs NAT

- Bridge: single subnet, upstream router provides DHCP, proxy‑ARP
- NAT: separate subnet, local DHCP, address translation
- Bridge is simpler for devices with static IPs
- NAT is safer, isolates clients